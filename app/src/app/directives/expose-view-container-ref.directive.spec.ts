import { ExposeViewContainerRefDirective } from './expose-view-container-ref.directive';

describe('ExposeViewContainerRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ExposeViewContainerRefDirective();
    expect(directive).toBeTruthy();
  });
});
